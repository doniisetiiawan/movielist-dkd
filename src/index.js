/* eslint-disable react/jsx-filename-extension */
import React from 'react';
import ReactDOM from 'react-dom';

const App = () => <h1>movieLists</h1>;

ReactDOM.render(<App />, document.getElementById('root'));
